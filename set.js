module.exports = {
    stats: {
        deaths: /^([^ ]*) (?:went up in flames)(.*)$/,
        killBef: /^([^ ]*) (?:killed) ([^ ]*)(.*)$/,
        killAft: /^([^ ]*) (?:walked into a cactus whilst trying to escape) ([^ ]*)(.*)$/,
        noStats: /^([^ ]*) (?:Giết) ([^ ]*)(.*)$/
    }
}
